
In order to get the result, you have to create the database and input the data.
For that, you have to:
1. run the `init.sql` script to create the database and the necessary tables.
2. run the `solution.sql` script to create the solution related entities.
3. run the `result.sql` script to get the result.

> [!NOTE]
> Make sure to replace `'Your input string here'` in the `input.sql` script with your actual input data.

[Open the task](https://adventofcode.com/2025/day/4) \
[Go to main](https://github.com/KerasirovED/AdventOfCode)